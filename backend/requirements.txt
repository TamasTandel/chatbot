Flask>=2.0
Flask-SQLAlchemy>=2.5
Flask-Migrate>=3.0
Flask-JWT-Extended>=4.0
Flask-Cors>=3.0
psycopg2-binary>=2.9 # For PostgreSQL
python-dotenv>=0.19
Werkzeug>=2.0 # For password hashing, often a Flask dependency
passlib>=1.7 # For more advanced password hashing like argon2, bcrypt
bcrypt>=3.2 # Specifically for bcrypt hashing
gunicorn # For running the app in production (optional for dev)
